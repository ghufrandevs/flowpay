import { AnimatedCounter } from './animated-counter';

describe('AnimatedCounter', () => {
  it('should create an instance', () => {
    const directive = new AnimatedCounter();
    expect(directive).toBeTruthy();
  });
});
